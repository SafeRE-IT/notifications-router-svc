log:
  disable_sentry: true

client:
  endpoint: http://...

keys:
  signer: ...

doorman:
  skip_sign_check: false

db:
  url: db_url

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: notifications-router-svc
  service_port: 80
